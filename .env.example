DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:5432/agenticcrm?schema=public"

# Signing key for the session cookie. Generate with: openssl rand -base64 32
AUTH_SECRET="replace-me-with-a-32-byte-random-string"
